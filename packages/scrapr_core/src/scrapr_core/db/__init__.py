"""Database layer: models, migrations and the session factory.

Importing `scrapr_core.db.models` is what populates `Base.metadata`; anything
that touches the schema — Alembic, tests, the API — goes through it rather than
importing individual model modules and hoping the set is complete.
"""
